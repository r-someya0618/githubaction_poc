name: Staging Deploy

on:
  push:
    branches:
      - develop

permissions:
  contents: read
  issues: write

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

  build:
    name: Build
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

  deploy:
    name: Deploy to Staging
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Staging: STG_SKIP_APPROVAL=true のときのみスキップ
      - name: Manual Approval
        if: vars.STG_SKIP_APPROVAL != 'true'
        uses: ./.github/actions/manual-approval
        with:
          approvers: ${{ vars.DEPLOY_APPROVERS }}
          issue-title: "STGデプロイ承認リクエスト - Run #${{ github.run_number }}"
          timeout-minutes: "30"

      - name: Deploy to Vercel Preview
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          github-comment: false

      - name: Deployment complete
        run: echo "Staging deployment completed successfully"
